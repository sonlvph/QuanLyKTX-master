<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quanlyktx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuanLyKTX">
        <activity android:name=".UpdateStudentActivity"></activity>
        <activity android:name=".ListStudentActivity"></activity>
        <activity android:name=".TangActivity" />
        <activity android:name=".RoomActivity" />
        <activity android:name=".SplashScreenActivity"></activity>
        <activity android:name=".StudentActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".CreatAccActivity" />
        <activity android:name=".AddStudentActivity">

        </activity>
        <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

    </application>

</manifest>